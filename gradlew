#!/usr/bin/env sh
######################################################################
# Gradle start up script (minimal stub). Ensure executable permission.
######################################################################
PRG="$0"
PRGDIR=`dirname "$PRG"`
exec "$PRGDIR/gradlew" "$@"
